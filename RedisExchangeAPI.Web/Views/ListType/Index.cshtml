@model List<string>

@{
    ViewData["Title"] = "Index";
}
<form method="post" i asp-action="Add" enctype="application/x-www-form-urlencoded">
    <input type="text name=name" />
    <button type="submit">Kaydet</button>
</form>

@foreach(var item in Model){

    <h2>@item.ToString()</h2>
}